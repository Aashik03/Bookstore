/** @type {import('tailwindcss').Config} */
export const content = [
  './src/**/*.{js,jsx,ts,tsx}', // Scan all JS, JSX, TS, TSX files in the src directory
  './public/index.html', // If you're using HTML files (optional)
];
export const theme = {
  extend: {},
};
export const plugins = [];
